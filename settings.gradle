pluginManagement {
    repositories {
        // Seu repositório offline local
        maven {
            url = java.nio.file.Paths.get(rootDir.absolutePath, 'local-repo').toUri().toURL()
        }
        // URL oficial corrigida da Fabric
        maven { 
            url = 'https://maven.fabricmc.net/' 
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'galaxite'